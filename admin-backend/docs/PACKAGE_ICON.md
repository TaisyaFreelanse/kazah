# Управление значком пакета

## Обзор

Каждый пакет имеет свой цвет значка, который используется для визуальной идентификации пакета в приложении.

## Выбор цвета значка

### В админ-панели

1. Откройте раздел "Пакетные вопросы"
2. Нажмите "Редактировать" на нужном пакете
3. В разделе "Цвет значка" вы можете:
   - Выбрать цвет визуально через color picker (клик по превью)
   - Ввести hex-код цвета вручную (например, `#4CAF50`)
4. Нажмите "Сохранить"

### Формат цвета

Цвет хранится в формате HEX:
- Примеры: `#4CAF50` (зеленый), `#9C27B0` (фиолетовый), `#795548` (коричневый)
- Формат: `#RRGGBB` (6 символов после #)
- Дефолтный цвет: `#4CAF50` (зеленый)

## Использование в приложении

### Получение цвета значка

Цвет значка передается через публичный API:

```dart
// Получить пакеты с цветами значков
final response = await http.get(
  Uri.parse('https://blim-bilem-admin-backend.onrender.com/api/public/packages'),
);

final packages = jsonDecode(response.body) as List;
for (var package in packages) {
  final iconColor = package['iconColor']; // Например: "#4CAF50"
  final packageName = package['name'];
  
  // Использовать цвет для отображения значка
}
```

### Отображение значка рядом с названием пакета

В экране магазина пакетов:

```dart
Container(
  decoration: BoxDecoration(
    color: Color(int.parse(package.iconColor.replaceFirst('#', '0xFF'))),
    shape: BoxShape.circle,
  ),
  child: Icon(Icons.extension, color: Colors.white),
)
```

### Отображение значка рядом с вопросом

Если вопрос взят из пакета, отображайте значок пакета рядом с вопросом:

```dart
// В модели Question должно быть поле packageId и packageIconColor
if (question.packageId != null) {
  Container(
    width: 20,
    height: 20,
    decoration: BoxDecoration(
      color: Color(int.parse(question.packageIconColor.replaceFirst('#', '0xFF'))),
      shape: BoxShape.circle,
    ),
  )
}
```

## Структура данных

### В базе данных
```sql
icon_color VARCHAR(7) DEFAULT '#4CAF50'
```

### В API ответе
```json
{
  "id": 1,
  "name": "Больше вопросов",
  "iconColor": "#9C27B0",
  "price": 1000,
  ...
}
```

## Рекомендации по цветам

Для лучшей визуальной идентификации рекомендуется использовать:
- **Больше вопросов**: `#9C27B0` (фиолетовый)
- **История Казахстана**: `#795548` (коричневый)
- **Другие пакеты**: выбирайте контрастные цвета для различия

## Примеры использования

### Flutter/Dart

```dart
// Конвертация HEX в Color
Color hexToColor(String hex) {
  return Color(int.parse(hex.replaceFirst('#', '0xFF')));
}

// Использование
final packageColor = hexToColor(package.iconColor);
Container(
  decoration: BoxDecoration(
    color: packageColor,
    borderRadius: BorderRadius.circular(8),
  ),
  child: Icon(Icons.extension, color: Colors.white),
)
```

### React (для админ-панели)

```jsx
<div 
  className="package-icon" 
  style={{ backgroundColor: package.iconColor }}
>
  📦
</div>
```

## Важные замечания

1. **Цвет сохраняется в базе данных** - изменения применяются сразу после сохранения
2. **Цвет передается в приложение** - через публичный API `/api/public/packages`
3. **Визуальный превью** - в админ-панели есть превью цвета для удобства
4. **Валидация** - цвет должен быть в формате HEX (#RRGGBB)

